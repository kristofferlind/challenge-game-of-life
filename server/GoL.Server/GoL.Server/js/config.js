(function(GAME, undefined) {
    'use strict';

    GAME.Config = {
        cellSize: 10,
        cellColor: '#000000',
        elements: {
            canvas: '#game'
        }
    };

}(window.GAME = window.GAME || {}))
